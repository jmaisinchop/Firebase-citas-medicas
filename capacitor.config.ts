import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Firebase-citas-medicas',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
